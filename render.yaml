services:
  - type: web
    name: render-supabase-mcp
    env: node
    buildCommand: npm install && npm install --save-dev @types/node @types/express && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production 